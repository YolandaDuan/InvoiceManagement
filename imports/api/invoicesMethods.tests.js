import { Meteor } from "meteor/meteor";

if (Meteor.isServer) {
    describe('Invoices', () => {
        describe('methods', () => {
            it('can delete owned invoie', () => {});
        });
    });
}